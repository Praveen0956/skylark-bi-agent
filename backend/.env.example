# Copy this file to .env and fill in real values.
# Used only for local testing -- on Render, paste these into the
# Environment tab instead (no file needed there).

MONDAY_API_TOKEN=
WORK_ORDERS_BOARD_ID=
DEALS_BOARD_ID=
ANTHROPIC_API_KEY=